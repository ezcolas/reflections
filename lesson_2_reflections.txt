What happens when you initialize a repository? Why do you need to do it?

 When you initialize you repository when the command git init you create a empty git repository.


How is the staging area different from the working directory and the repository? what value do you think it offers.

The staging area is an intermediate step between the working directory and the repository. It gives you access to add the file that you want before the commit.